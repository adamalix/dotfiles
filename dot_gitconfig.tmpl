[user]
    email = {{ .email | quote }}
    name = {{ .name | quote }}
[core]
    editor = zed --wait --new
    excludesfile = ~/.gitignore
[merge]
    conflictstyle = diff3

[include]
    path = ~/.gitconfig_local
